--!strict
-- LutGrades
--
-- GENERATED by references/tools/lut_fit.py from .cube files. Do not edit by
-- hand; regenerate with:
--   python lut_fit.py <luts>/*.cube --out LutGrades.luau
--
-- Each grade is the closest Roblox gets to its LUT: an exposure shift (added
-- to Lighting.ExposureCompensation) and ONE ColorCorrectionEffect (contrast,
-- saturation, brightness, tint). shadowTint and highlightTint are the split
-- toning a CC cannot express: multiply them into the ambient and sun colours.
-- rmsError is the fit error in 0..255 levels. Pass `grades` to
-- ColourGrade.start, which blends and scales them.

export type Grade = {
	exposure: number,
	contrast: number,
	saturation: number,
	brightness: number,
	tint: Color3,
	shadowTint: Color3,
	highlightTint: Color3,
	rmsError: number,
	source: string,
}

local grades: { [string]: Grade } = {
	neutral = {
		exposure = 0,
		contrast = 0,
		saturation = 0,
		brightness = 0,
		tint = Color3.new(1.0000, 1.0000, 1.0000),
		shadowTint = Color3.new(1.000, 1.000, 1.000),
		highlightTint = Color3.new(1.000, 1.000, 1.000),
		rmsError = 0,
		source = "neutral.cube",
	},
	golden_hour = {
		exposure = 0.1074,
		contrast = -0.0318,
		saturation = 0.0021,
		brightness = 0.0019,
		tint = Color3.new(1.0000, 0.9913, 0.9357),
		shadowTint = Color3.new(0.982, 1.148, 0.994),
		highlightTint = Color3.new(1.021, 0.990, 0.976),
		rmsError = 8.93,
		source = "golden_hour.cube",
	},
	moonlight = {
		exposure = 0.1032,
		contrast = -0.0464,
		saturation = -0.2079,
		brightness = -0.0253,
		tint = Color3.new(0.9416, 0.9528, 1.0000),
		shadowTint = Color3.new(0.866, 0.931, 1.099),
		highlightTint = Color3.new(1.029, 0.996, 0.972),
		rmsError = 5.28,
		source = "moonlight.cube",
	},
	neon_night = {
		exposure = 0.1804,
		contrast = 0.2049,
		saturation = 0.0938,
		brightness = 0.0096,
		tint = Color3.new(0.9522, 0.8868, 1.0000),
		shadowTint = Color3.new(0.916, 1.059, 1.060),
		highlightTint = Color3.new(1.006, 0.975, 0.997),
		rmsError = 8.37,
		source = "neon_night.cube",
	},
	overcast = {
		exposure = 0.0349,
		contrast = -0.1355,
		saturation = -0.2693,
		brightness = 0.0224,
		tint = Color3.new(0.9975, 0.9871, 1.0000),
		shadowTint = Color3.new(1.026, 0.988, 1.014),
		highlightTint = Color3.new(1.000, 0.995, 0.991),
		rmsError = 9.24,
		source = "overcast.cube",
	},
}

local order: { string } = { "neutral", "golden_hour", "moonlight", "neon_night", "overcast" }

return {
	grades = grades,
	order = order,
}
